I solved this the same way as `objdumpme`. Loaded the binary in `gdb`, broke on `strcmp`, and inspected the arguments it was comparing. From there, I grabbed the correct string, passed it as input, it was the flag.

Didn’t need the hints at all. Im probably not solving them the intended way, but still got the flags.
