The next challenge required connecting to a remote shell server using netcat: `shell.hackintro25.di.uoa.gr` on port `31407`. I used the `nc` command to establish the connection and retrieve the flag. While I’m still wrapping my head around what exactly netcat does under the hood, I understand it as a basic tool for making raw network connections—like opening a direct line to a remote server. This challenge was likely aimed at introducing that concept in a hands-on way.
