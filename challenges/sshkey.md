For this challenge, I already had an SSH keypair ready, since I’d previously set it up for GitHub. I reused the same public key. On the shell server, I created the `~/.ssh/authorized_keys` file and added my public key to it. After that, I connected to `shell.hackintro25.di.uoa.gr` over SSH from my terminal, and was greeted with the message:

_“Congratulations on setting up SSH key authentication!  
Here is your flag: secure_your_shells_with_crypto”_
